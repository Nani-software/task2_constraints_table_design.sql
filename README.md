Task 2 - Data Types, Constraints & Table Design

This task focuses on proper table design using SQL constraints and data types.

Key Concepts Used:
- INT, VARCHAR, DATE, TIMESTAMP data types
- PRIMARY KEY, UNIQUE, NOT NULL, CHECK constraints
- ALTER TABLE (ADD, RENAME, DROP column)
- Handling constraint violations

Outcome:
Learned how constraints ensure data integrity and proper schema design.# task2_constraints_table_design.sql
